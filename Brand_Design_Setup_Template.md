# Brand_Design_Setup_Template.md: 브랜드 디자인 셋업 자동 생성 템플릿

이 문서는 `Concept_partner.md`에서 길어 올린 컨셉 대화를 바탕으로, **`{브랜드코드명}_Web_Plan.md`의 1단계-2 디자인 스타일 가이드 섹션에 폰트·컬러 토큰을 자동으로 채워 넣기** 위한 내부 생성 가이드다.

별도 파일을 새로 만들지 않는다. 변환 결과는 모두 `{브랜드코드명}_Web_Plan.md` 안으로 들어간다. `Web_Plan_Template.md` 원본은 절대 수정하지 않는다.

> 📌 이 파일을 실행하는 주체: **에이전트(AI)**
> 📌 사용자에게 직접 받는 입력: **브랜드 표시명(한글/영문)과 코드용 영문명 두 가지**
> 📌 그 외 모든 정보: `Concept_partner.md` 대화 결과에서 자동 추출
> 📌 출력 결과: `{브랜드코드명}_Web_Plan.md`의 1단계-2 섹션 완성 (원본 템플릿 보존)
> 📌 호출 시점: `{브랜드코드명}_Web_Plan.md`의 나머지 섹션이 채워진 직후

---

## 00. 시작 시 첫 행동

이 파일이 로드되면, 에이전트는 사용자에게 **두 가지**를 확인한다.

> "마지막으로 두 가지만 확인할게요^^
>
> ① **브랜드 표시명**: 사이트에 실제로 보여질 이름이에요. 한글이든 영문이든 모두 괜찮아요.
> (예: 디노클래스, 소라의 정원, Dino Class, Studio Rosa)
>
> ② **코드용 영문명**: Tailwind 설정의 컬러·폰트 변수 이름에 쓰이는 짧은 영문이에요. 영문 소문자와 숫자만 가능해요.
> (예: dinoclass, sorasgarden, studiorosa)
>
> 브랜드명이 이미 영문이라면 그대로 변환해드릴게요. 한글 브랜드명이라면 제가 코드명 후보를 추천해드릴 수도 있어요."

두 가지를 받으면, 더 묻지 않는다. **나머지 모든 결정(컬러·폰트·모드)은 지금까지의 컨셉 대화에서 추출하여 자동으로 채운다.**

---

## 01. AI를 위한 변환 가이드

이 섹션은 사용자에게 보여지지 않는다. 에이전트가 이 가이드를 읽고 변환을 수행한다.

### 1.1 입력 출처

다음 두 가지를 입력으로 사용한다:
1. **사용자가 방금 입력한 영문 브랜드명** (예: "Dino Class")
2. **Concept_partner 대화 결과**, 특히 04번 '🎨 분위기와 색을 입히기' 섹션의 답변

### 1.2 분위기 → 컬러 톤 매핑

사용자가 묘사한 공간/분위기를 컬러 톤으로 옮긴다.

| 사용자 표현 (예시) | 톤 추론 | 모드 권장 |
|---|---|---|
| "따뜻한 동네 책방", "햇살 드는 카페", "할머니 집 거실" | 베이지·우드톤·따뜻한 화이트 | 라이트 |
| "햇살 드는 미니멀 카페", "조용한 갤러리" | 무채색 위주, 밝은 베이스, 작은 채도 포인트 | 라이트 |
| "늦은 밤의 라운지", "재즈 바", "서재의 밤" | 다크 네이비·블랙·골드 포인트 | 다크 |
| "검은 칠판", "모던 IT 회사", "사이버 공간" | 블랙·네온 포인트 컬러 | 다크 |
| "활기찬 시장", "축제", "어린이 놀이공간" | 비비드·보색 대비·컬러풀 | 라이트 |
| "조용한 도서관", "차분한 갤러리", "겨울의 숲" | 채도 낮은 중간톤, 단정한 무채색 | 라이트 또는 다크 |

추론이 모호하면, 사용자가 명시한 색깔의 평균 명도를 기준으로 다크/라이트를 결정한다.

### 1.3 색상 묘사 → 헥스 코드 매핑

사용자가 표현한 색을 실제 헥스 코드로 변환한다. **사용자의 단어 표현을 살려서 컬러 변수명도 정한다.**

| 사용자 표현 (예시) | 추정 헥스 | 가능한 변수명 |
|---|---|---|
| "바닷가 모래색" | `#e8d4b0` | `sand` |
| "늦가을 단풍색" | `#c25c3c` | `maple` |
| "흐린 하늘색" | `#a8b8c8` | `mist` |
| "오래된 종이색" | `#f5efe6` | `parchment` |
| "갈색 가죽" | `#8b6f47` | `leather` |
| "눈 내린 아침" | `#f8f9fa` | `snow` |
| "진한 주황" | `#c2410c` | `ember` |
| "어두운 적갈색" | `#5c2e2e` | `wine` |
| "짙은 남색" | `#1a2540` | `midnight` |
| "민트 잎" | `#7dd3a5` | `mint` |
| "네온 노랑" | `#fee800` | `spark` |

3가지 색상이 답변에 있으면, 그 셋을 Primary / Secondary / Accent로 배치한다. 사용자가 비율이나 우선순위를 명시하지 않았으면, **가장 먼저 언급한 색**을 Primary로 둔다.

### 1.4 부족한 색상의 자동 보강

사용자가 색을 3개만 답했어도, 디자인 셋업에는 다음 5종이 필요하다:

1. **Primary** — 핵심 브랜드 컬러
2. **Background** — 가장 어두운(다크 모드) 또는 가장 밝은(라이트 모드) 배경
3. **Surface** — 카드/섹션 배경 (Background보다 한 단계 다른 톤)
4. **Text Main** — 메인 텍스트
5. **Text Sub** — 서브 텍스트, 비활성 요소

부족한 색은 Primary와 분위기에 어울리도록 에이전트가 자동 보강한다. **Tailwind의 zinc / slate / neutral / stone 팔레트를 기준**으로 잡으면 안전하다.

| 모드 | Background | Surface | Text Main | Text Sub |
|---|---|---|---|---|
| 다크 (zinc 기반) | `#09090b` | `#18181b` | `#f4f4f5` | `#71717a` |
| 라이트 (zinc 기반) | `#fafafa` | `#f4f4f5` | `#18181b` | `#71717a` |
| 따뜻한 라이트 (stone 기반) | `#fafaf9` | `#f5f5f4` | `#1c1917` | `#78716c` |
| 차가운 다크 (slate 기반) | `#020617` | `#0f172a` | `#f1f5f9` | `#64748b` |

### 1.5 폰트 기본값

사용자가 폰트를 따로 명시하지 않은 경우, 분위기에 따라 다음을 기본으로 한다:

| 분위기 | sans (본문) | mono (포인트) |
|---|---|---|
| 따뜻한 / 라이트 | `Inter` | `JetBrains Mono` |
| 다크 / 미래적 | `Inter` | `Chakra Petch` |
| 부드러운 / 손글씨 | `Pretendard` | `IBM Plex Mono` |
| 단정한 / 전통적 | `Noto Sans KR` | `Noto Serif KR` (serif 사용) |
| 캐주얼 / 활기찬 | `Pretendard` | `Space Mono` |

한글이 메인 콘텐츠라면, 한글 친화 폰트(`Pretendard`, `Noto Sans KR`)를 우선 고려한다.

### 1.6 변수명 규칙

표시명(사이트에 보이는 이름)과 코드명(Tailwind 변수 이름)은 별개다.

**코드명 생성 규칙:** 사용자가 입력한 코드용 영문명을 소문자 처리하고 공백·특수문자를 제거한다.

| 브랜드 표시명 | 코드용 영문명 (사용자 입력 또는 추천) | Tailwind 네임스페이스 |
|---|---|---|
| Dino Class | dinoclass | `dinoclass` |
| Sora's Garden | sorasgarden | `sorasgarden` |
| Studio Hama | studiohama | `studiohama` |
| 디노클래스 | dinoclass (추천) | `dinoclass` |
| 소라의 정원 | sorasgarden (추천) | `sorasgarden` |
| 하루스튜디오 | harustudio (추천) | `harustudio` |

한글 브랜드명의 코드명 추천 원칙:
- 발음을 영문으로 옮기거나 (디노 → dino, 하루 → haru)
- 뜻을 영어로 옮기거나 (정원 → garden)
- 둘을 조합한다 (소라의 정원 → sorasgarden)
- 추천 후 사용자에게 확인받는다: "코드명으로 `sorasgarden` 어떠세요?"

내부 컬러 변수명은 위 1.3에서 추출한 단어를 우선 쓰고, 자동 보강된 색에는 일반명(`background`, `surface`, `textMain`, `textSub`)을 사용한다.

### 1.7 로고 추천 원칙

분위기·브랜드명·Primary 색상을 종합해 로고를 추천한다.

**아이콘 선택 기준**

| 분위기 | 추천 아이콘 예시 |
|---|---|
| 전문적·신뢰감 | 🎯 👑 💡 ⭐ |
| 따뜻함·교육·지식 | 📚 🌱 ✨ 🕯 |
| 창의적·독창적 | 🎨 🔮 🦄 🌈 |
| 기술·미래·디지털 | ⚡ 🚀 💎 🔷 |
| 자연·힐링·감성 | 🌿 🌸 🍃 🌙 |

**텍스트 표기 형식**

| 모드·분위기 | 추천 형식 |
|---|---|
| 다크 모드, 강한 임팩트 | ALL CAPS (예: DINO CLASS) |
| 라이트 모드, 친근함 | TitleCase (예: Dino Class) |
| 미니멀, 세련됨 | 소문자 (예: dino class) |
| 한글 브랜드명 | 그대로 표기 (예: 디노클래스) |

**로고 구성 기본 원칙**
- 아이콘에 Primary 색상 적용 → 브랜드 컬러를 로고에서 첫 인상으로
- 텍스트 색상은 Text Main 사용
- 아이콘 + 텍스트 조합이 가장 인식률 높음
- 아이콘은 이모지를 우선 추천 (SVG 아이콘이 필요하면 Lucide Icons 또는 Heroicons 스타일 명시)

---

## 02. 출력 양식

다음 형식으로 **`Web_Plan_Template.md`의 1단계-2 디자인 스타일 가이드 섹션을 채운다.** `{...}` 부분은 위 변환 가이드를 따라 자동으로 채운다.

- `{브랜드 표시명}` = 사이트에 보여지는 브랜드명 (한글/영문 모두 가능)
- `{브랜드코드명}` = Tailwind 변수에 쓰이는 영문 소문자 코드명

````markdown
# {BrandName}_Design_Setup.md: {브랜드명} 디자인 셋업

이 파일은 **{브랜드명}** 브랜드의 시각적 정체성을 정의한다.
바이브코딩 도구에서 홈페이지를 만들 때, `Web_Plan_Template.md`와 함께 로드되어 일관된 시각 언어를 유지한다.

> 📌 사용 시점: 코드 작성 시 컨텍스트로 함께 제공
> 📌 갱신 주기: 브랜드가 변하지 않는 한 그대로 재사용

---

## 1. 폰트 설정

### {sans 폰트명} — 메인 폰트
- **사용처:** 본문, 제목, 일반 UI 텍스트
- **느낌:** {폰트가 주는 인상 한 줄}

### {mono 또는 serif 폰트명} — 포인트 폰트
- **사용처:** 헤더의 영문/숫자, 라벨, 강조 태그
- **느낌:** {폰트가 주는 인상 한 줄}

### Tailwind 폰트 토큰

```javascript
fontFamily: {
  sans: ['{sans 폰트명}', 'sans-serif'],
  mono: ['{mono 폰트명}', 'monospace'],
}
```

---

## 2. 브랜드 컬러 시스템

**기본 모드:** {라이트 또는 다크}
**컨셉 대화에서 추출한 분위기:** "{사용자가 답한 분위기 표현 그대로}"

### 🎯 Core Identity (핵심 정체성)

**{브랜드명} {Primary 변수명, 첫 글자 대문자}: `{HEX1}`**
- **이름의 뜻:** {왜 이 변수명을 골랐는지 한 줄 — 사용자 표현 인용}
- **사용처:** 메인 CTA 버튼, 핵심 강조 텍스트, 호버 이펙트, 드래그 선택 영역
- **호버 이펙트 유형:**
  - 기본: 요소 색상이 Primary로 전환 (color / border-color transition)
  - 심화: 요소가 살짝 떠오르는 효과 (translateY -4px + box-shadow 강화)
  - 고급: 마우스 방향으로 기울어지는 3D 틸트 (CSS perspective + rotateX/Y)
- **사용 비율:** 화면 전체의 5~10% 내외 (포인트로 강하게 작용)

### 🌑 Background & Surface (배경)

**{브랜드명} Background: `{HEX2}`**
- **사용처:** 메인 배경. 페이지 전체의 베이스
- **사용 비율:** 60~70%

**{브랜드명} Surface: `{HEX3}`**
- **사용처:** 섹션 박스, 카드 배경, FAQ 아코디언 등 Surface 요소
- **사용 비율:** 20~30%

### ✏️ Typography & Elements (텍스트와 요소)

**{브랜드명} Text Main: `{HEX4}`**
- **사용처:** 메인 텍스트 (제목, 본문, 설명)

**{브랜드명} Text Sub: `{HEX5}`**
- **사용처:** 서브 타이틀, 설명 문구, 비활성 요소, 메타 정보

### Tailwind 컬러 토큰

```javascript
colors: {
  {brand_lowercase}: {
    {primary 변수명}: '{HEX1}',  // {짧은 설명}
    background:        '{HEX2}',
    surface:           '{HEX3}',
    textMain:          '{HEX4}',
    textSub:           '{HEX5}',
  }
}
```

---

## 3. 사용 원칙

### 1) {라이트/다크} 모드를 기본으로 한다
{브랜드명}은 **{한 줄 컬러 사용 원칙 — 사용자가 묘사한 분위기를 기반으로 작성}**.

### 2) {Primary 변수명}의 사용은 신중하게
`{HEX1}`은 채도가 {높은/중간/낮은} 편이다. 너무 많이 쓰면 인상이 흐려지고, 너무 적게 쓰면 브랜드 색이 안 보인다. **CTA 버튼, 핵심 강조, 호버 상태**에 집중적으로 사용한다.

### 3) 호버 이펙트는 Primary와 함께 단계적으로
단순히 색이 바뀌는 것을 넘어, 원하는 인터랙션 수준을 선택한다.
- **기본**: 색상 전환만 (transition: color 0.2s)
- **심화**: 색상 + 살짝 떠오르기 (translateY -4px + box-shadow 강화) — 카드·버튼에 적합

> ⚠️ 3D 틸트(마우스 방향으로 기울어지는 효과)는 적용하지 않는다. 모바일·일부 브라우저에서 레이아웃이 깨질 수 있다.
> 원하는 레벨을 `Web_Plan_Template.md`의 '인터랙션 & 애니메이션' 항목에 명시하면 바이브코딩 도구가 해당 JS/CSS를 자동으로 가져와 구현합니다.

### 3) 회색조의 위계를 지킨다
- Background `{HEX2}` → Surface `{HEX3}`: 미세한 명도 차로 박스/카드를 구분
- Text Main `{HEX4}` → Text Sub `{HEX5}`: 본문과 보조 텍스트의 명확한 구분

### 4) 명도 대비를 지킨다
{Primary 색}을 {대비 부족 색}과 짝짓지 않는다. 항상 {권장 짝} 위에 둔다.

---

## 4. 로고 디자인

**구성:** {아이콘 + 텍스트 / 텍스트만 / 아이콘만}
**아이콘:** {추천 아이콘 — 1.7 로고 추천 원칙 기반}
**텍스트:** {브랜드 표시명} — {ALL CAPS / TitleCase / 소문자 / 한글 그대로}
**폰트:** {mono 또는 sans — 위에서 정의한 폰트와 연계}
**색상:** 아이콘 → Primary `{HEX1}` / 텍스트 → Text Main `{HEX4}`
**배치:** {헤더 좌측 / 헤더 중앙}

> 💡 바이브코딩 프롬프트에 "헤더 로고는 {아이콘} {브랜드 표시명} 조합으로, 아이콘 색은 Primary({HEX1}), 폰트는 {폰트명}으로 구현해 줘"라고 그대로 붙여 넣으면 자동 구현됩니다.

---

## 5. 바이브코딩 사용 안내

이 파일을 코딩 도구에 함께 로드한 뒤, 다음과 같이 지시한다:

> *"`Web_Plan_Template.md`의 디자인 스타일 가이드와 본 파일(`{BrandName}_Design_Setup.md`)을 결합해서 코드를 작성해 줘. Tailwind 설정의 `colors`와 `fontFamily`는 본 파일의 토큰을 그대로 가져오고, 페이지의 분위기·구조·콘텐츠는 Web_Plan_Template을 따라. 두 파일이 충돌할 경우, 본 파일(디자인 셋업)이 우선한다."*
````

---

## 03. 사용 예시 (참고용)

### 입력
- **컨셉 대화 04번 답변:**
  - 분위기: "햇살 드는 미니멀 카페"
  - 3가지 색: "오래된 종이색", "단풍 진 다음의 회색", "강조는 진한 주황"
- **사용자가 입력한 브랜드명:** "Sora's Garden"

### 출력 (`SorasGarden_Design_Setup.md`의 컬러 부분 미리보기)

```javascript
colors: {
  sorasgarden: {
    parchment:  '#f5efe6',  // 메인 배경 (오래된 종이색)
    surface:    '#ebe5db',  // 카드 배경 (살짝 어두운 종이톤)
    ash:        '#5e5e5e',  // 본문 텍스트 (단풍 진 후 회색)
    mute:       '#9a9a9a',  // 보조 텍스트
    ember:      '#c2410c',  // 핵심 브랜드 컬러 (진한 주황)
  }
}
```

폰트는 '햇살 드는 미니멀 카페' 분위기 추론에 따라 라이트/따뜻한 톤으로 판단 → `Inter` + `JetBrains Mono` 적용.
모드는 "오래된 종이색"이 가장 먼저 언급된 점을 토대로 라이트 모드로 결정.

---

## 04. AI를 위한 운영 프로토콜

### 1. 사용자에게 딱 한 번만 묻는다
영문 브랜드명. 그 외 어떤 것도 다시 묻지 않는다. 모르는 건 위 변환 가이드로 추론한다.

### 2. 추론은 보여준다, 따지진 않는다
변환이 끝나면, 에이전트는 사용자에게 **간단한 요약**을 보여주고 확인받는다.

> "✨ {브랜드명} 디자인 셋업이 완성됐어요^^
> - 모드: {라이트/다크}
> - 메인 컬러: {Primary 색명} ({HEX1})
> - 폰트: {sans} + {mono}
>
> 마음에 드세요? 컬러나 폰트를 바꾸고 싶으시면 한 마디만 해주세요."

### 3. 사용자가 수정 요청 시
한 항목만 바꾸도록 받는다 — "Primary를 좀 더 진하게" / "폰트를 한글 친화적으로" 등. 한 번에 다 갈아엎지 않고, 점진적으로 다듬는다.

### 4. 사용자가 OK 하면
확정된 폰트·컬러 토큰을 **`{브랜드코드명}_Web_Plan.md`의 1단계-2 디자인 스타일 가이드 섹션**에 최종으로 채워 넣는다. 원본 `Web_Plan_Template.md`는 수정하지 않는다.

채워 넣은 뒤, 다음 안내로 마무리한다.

> "✅ 디자인 토큰이 기획서 안에 담겼어요. 이제 `{브랜드코드명}_Web_Plan.md` 파일 하나를 들고 바이브코딩을 시작하시면 돼요. 좋은 사이트가 만들어지길 응원할게요!"
